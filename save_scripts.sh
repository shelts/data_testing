git add *.cpp *.sh *.py *.h
git commit -m "added new structs pos and vel. the bodies struct now have those in it. The way functions are called are also changed so that the pos struct is sent in and not just r"
git push